# Codex handoff — take over the Claude v2 forecasting candidate (SIH26170)

Paste the block under "Prompt for Codex" into Codex in the main project workspace. It is written to be short; the detail lives in the files it points to.

## Where things are

- Claude's workspace: `../claude_forecast_v2_pack/` (sibling folder of this project; extracted from `outputs/CLAUDE_FORECASTING_TRAIN_ONLY_PACK.zip`). Also packaged as `outputs/CLAUDE_FORECAST_V2_DELIVERABLE.zip` (new files only).
- Full handoff: `claude_forecast_v2_pack/docs/CLAUDE_FORECAST_V2_HANDOFF.md` (read this first, about 5 minutes).
- Results: `claude_forecast_v2_pack/outputs/claude_forecast_v2/COMPARISON.md`, `oof_predictions.csv`, `interval_experiment__*.json`, `candidate/xgb_abs_resid_leak/`.
- Status and disclosures: `claude_forecast_v2_pack/outputs/claude_forecast_v2/STATUS.md`.

## Prompt for Codex

You are Karthik's Codex collaborator on SIH26170 (MLCC burn-in screening: anomaly detection plus a 168 h leakage forecast from 0 h and 24 h readings; all data synthetic). You own dataset integrity, anomaly-detector improvement, false-alert analysis, independent evaluation, final calibration, model-selection review and integration into the shared release. Claude, in a separate training-only workspace, has finished a v2 forecasting candidate. Your next task is to integrate and independently test it. Do not retrain or tune it yourself; evaluate it.

What Claude delivered (paths relative to `../claude_forecast_v2_pack/`, read-only for you except where you copy files in):
- `src/sih26170/forecast_v2.py`, `scripts/claude_forecast_v2.py`, `tests/test_forecast_v2.py` (new files; shared modules untouched).
- `outputs/claude_forecast_v2/candidate/xgb_abs_resid_leak/` (recommended candidate: `manifest.json`, `model.ubj`, `model.json`) and `candidate/xgb_abs_resid_aux/` (a statistical tie; ignore unless you want to compare).
- `docs/CLAUDE_FORECAST_V2_HANDOFF.md` (recommendation, tradeoffs, interface, integration steps), `outputs/claude_forecast_v2/COMPARISON.md`, `PREDECLARED_COMPARISON.md`, `DIAGNOSTICS.md`, `STATUS.md`, `oof_predictions.csv` (86,400 keyed out-of-fold forecasts), `interval_experiment__*.json`.

Key facts (training-fold development results, 36 training batches, not final validation):
- Candidate = XGBoost with `reg:absoluteerror` on the correction over persistence (`base_margin` = 24 h value / limit), v1 hyperparameters, the ten leakage-only 0/24 h features v1 already computes, one shared model for all four profiles, target = observed final_value / upper_limit as in v1, clipped at zero only.
- Pooled normalized MAE 0.1429 vs persistence 0.1592 vs v1 XGBoost 0.2100; beats persistence in 5/5 folds and 35/36 batches (paired delta −0.016, SE 0.0017). Healthy-part MAE 0.0165 (persistence 0.0078, v1 0.116). Observed-crossing recall 0.315 vs 0.186 at FPR 0.0057 vs 0.0020 (38 vs 13 false alarms); 81 of 450 new crossings caught vs 0; latent recall excluding tester faults 0.216 vs 0.020.
- The reason v1 lost: squared error predicts the conditional mean of a heavy-tailed target and inflates 78 % healthy forecasts by +0.116; every squared-error configuration (including log1p targets) lost to persistence in 0/36 batches. Auxiliary and channel-peer features changed MAE by ≤ 0.0002.
- Unpredictable for every configuration: 357 late-onset defects and 77 onset-72 h tester faults (about 36 % of persistence error, 28 % of crossings). Slightly worse than persistence on intermittent-leakage and channel-fault parts.
- Interval proposal: stratified (slope robust z < 2 / 2–5 / ≥ 5) asymmetric signed-residual conformal quantiles plus a one-sided 90 % upper bound; on nested calibration (rotated 8-batch calibration sets) it kept 90 % coverage (fold range 0.86–0.93) and raised non-healthy coverage from 0.56 (v1's symmetric radius) to 0.75; per-batch coverage 0.84–0.96; quiet-stratum one-sided upper margin about 0.40 of the limit. The candidate returns point forecasts only; do not reuse v1's symmetric radius.
- Verification already done by Claude (six independent passes: leakage, folds, metric recomputation, inference contract, tests, integration into a copy of this project): no blocker; original 82 tests plus the new ones pass in this project's `.venv`; results reproduced bit-identically after fixes. Details in the handoff, section 11.

Your tasks, in order:
1. Copy the three new files and the recommended candidate folder into this project at the same relative paths (`sih26170/__init__.py` needs no change; the editable install picks the module up directly). Run the full test suite. Load the candidate with `from sih26170.forecast_v2 import load_forecast_candidate` and predict on `outputs/mlcc_v1/demo_early.csv` as a functional check. If you re-run the runner here, pass `--data-dir outputs/mlcc_v1` (the default `data/` is the pack layout). The loader enforces exact numpy/pandas/scikit-learn/xgboost versions as a hard error.
2. Independent evaluation on batches Claude never saw: the calibration and test batches under `outputs/mlcc_v1/` and a NEW untouched evaluation set you generate (the existing test set was inspected during v1). Compute normalized MAE, MAE in µA by profile, crossing confusion counts in three strata (already ≥ limit at 24 h; new crossings below the limit at 24 h; latent `is_future_failure` excluding tester faults), healthy-part MAE, per-batch paired deltas against persistence. `forecast_v2.forecast_metrics(pred_normalized, features, labels)` reproduces Claude's definitions; `labels` needs a `y_normalized` column. Compare with persistence and the v1 XGBoost on identical batches. Treat differences under about 0.01 normalized MAE as ties. Expect some shrinkage from the training-fold numbers.
3. Stress sets: run the candidate on `stress_low_prevalence_*` and `stress_unseen_condition_*`. Expectation to confirm or refute: low prevalence favours this candidate over v1 (v1's healthy bias is prevalence-driven); shifted conditions are neutral because no condition column is a feature.
4. Contract details: the output has `status`, `predicted_final_value` (µA), `weak_peer_warning`, `imputed_feature_count` and `unavailable_reason`; `candidate.predict_records(early)` gives JSON-safe dicts. `profile_id` is required and must match the manifest's supported profiles; a batch whose parts disagree on profile or limit rejects the whole upload (v1 behaviour), so split uploads by batch in the API if partial scoring is wanted. Pass measurement values as float64 exactly as measured (tree forecasts are piecewise constant; float32 or 6-digit rounding can move a part by up to 0.07 of the limit). `PredictionResult` needs `Optional` bounds or your calibrated bounds before construction. Full list in the handoff, section 9.
5. Calibration on the calibration batches only: stratified asymmetric quantiles and the one-sided upper margin of the candidate's residuals (helpers: `forecast_v2.stratum_index`, `signed_conformal_bounds`, `conformal_radius`), coverage reported per batch and per profile on the test batches, then connect to `PredictionResult`. State batch dependence and that bounds are not failure probabilities.
6. Model-selection review: decide between persistence and the candidate on your independent numbers, using the healthy-part gate and the false-alarm count as explicit tradeoffs. Note Claude's predeclared rule literally picks `xgb_abs_resid_aux` (both it and `xgb_abs_resid_leak` are saved; they differ by 0.00017 MAE, a tie); the leakage-only recommendation is a disclosed parsimony choice you may overrule. Review whether `recommend_action` semantics should change now that predicted crossings are rarer and more precise (precision 0.82 vs 0.71 on training folds); do not change them silently.
7. Anomaly-detector angle (yours): Claude's channel-peer statistic (leave-one-out fraction of same-batch same-channel peers with `current_batch_robust_z` > 3; formula in `forecast_v2.V2_FEATURE_FORMULAS`) flags all 113 visible instrument faults with zero false positives on training data and separates instrument faults from real drift among high-z parts. It did not help the forecast; consider it for a "suspected channel fault" reason code in the anomaly/decision layer.

Rules: keep v1 artifacts intact; never use `true_final_value` as a target; join labels only on `component_id, batch_id, component_family, measurement_name`; whole batches stay together in every split; report exactly which files and batches each number came from; record what you actually ran.
